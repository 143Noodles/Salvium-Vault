# Dependencies
node_modules/

# Build output (rebuilt for deployment)
dist/
dist-extension/

# WASM build directory (source files, not compiled output)
wasm-build/

# Environment files (keep .env.example if it exists)
.env
.env.*
.env.local
.env.*.local
.env.development
.env.production

# IDE and editor configs
.vscode/
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/

# OS files
.DS_Store
.DS_Store?
._*
Thumbs.db
ehthumbs.db
Desktop.ini

# Logs
*.log
!deploys.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Test coverage
coverage/
*.lcov
.nyc_output/

# Temporary files
*.tmp
*.temp
temp_*.cpp
temp_*.js
temp_*.ts
*.bak
*.bak-*
*.orig

# Debug files
*.stackdump

# TypeScript cache
*.tsbuildinfo

# Vite cache
.vite/

# Optional: WASM binaries (large files - uncomment if you want to exclude)
# Note: wallet/*.wasm files are needed for the app to work
# If you want to track them in git (recommended), leave this commented
# wallet/*.wasm

# ESLint cache
.eslintcache

# Stylelint cache
.stylelintcache
public/wallet/
LOCAL_NOTES.md

# Local backups and artifacts
App.tsx.backup
server.cjs.bak-*
public/salvium-vault.apk
wallet/backups/

# Android release secrets and generated outputs
*.jks
*.keystore
android/local.properties
android/**/*.apk
android/**/*.aab
android/**/output-metadata.json
android/**/.gradle/
android/**/build/
android/app/release/
android/app/debug/
scripts/qa/android-webview-harness/build/

# Desktop shell generated outputs
desktop/node_modules/
desktop/build/*
!desktop/build/icon.png
!desktop/build/icon.ico
!desktop/build/installerHeader.bmp
!desktop/build/installerSidebar.bmp
!desktop/build/apparmor-profile
!desktop/build/deb-postinst.sh
desktop/dist/
desktop/release/
desktop/content-dist/

# Local WASM/public staging copies
public/SalviumWallet.js
public/SalviumWallet.wasm

# Runtime data, caches, agent state - never commit (blockchain cache + telemetry logs live here)
data/
.gradle/

# Local audit/cleanup artifacts (not for public repo)
.local-backups/
server-patches/

# Stray orphan duplicates at repo root (the app imports from components/*, not these)
/AssetsPage.tsx
/Dashboard.tsx
/HistoryPage.tsx
/SettingsPage.tsx

# One-off debug scripts tied to removed diagnostics (not part of the app)
scripts/debug-target-mint.cjs
scripts/summarize-block-490710.cjs
scripts/verify-protocol-mint-recovery.cjs

# Android release artifacts must not be published by the web wallet
public/*.apk

# Local pre-change snapshots and working notes (never publish)
*.before-*
dist-backups/
wallet/backup-*/
wallet-legacy/backup-*/
INVESTIGATION_BRIEF.md
stakeable-balance-deploy-ts.txt

# Bundled Android build output
dist-android/
docker-compose.yml.assetrefs-*
salvium-tools-logo.png
